<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up or transferred. The encrypted preferences are keyed by
  a Keystore key that never leaves the device, so a restored copy can't be
  decrypted; the SQLite databases are caches rebuilt from the server and the
  device gallery. allowBackup="false" covers API < 31, these rules API 31+.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
