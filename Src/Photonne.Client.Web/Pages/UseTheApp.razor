@page "/use-the-app"
@layout LoginLayout
@inject IAuthService AuthService
@inject NavigationManager Navigation

<PageTitle>Usa la app de Photonne</PageTitle>

<div class="d-flex flex-column align-center justify-center" style="min-height: 100svh; padding: 24px;">
    <MudPaper Class="pa-8" Elevation="0" Outlined="true" Style="max-width: 460px; width: 100%; text-align: center;">
        <MudIcon Icon="@Icons.Material.Outlined.PhoneIphone" Color="Color.Primary" Style="font-size: 4rem;" Class="mb-3" />

        <MudText Typo="Typo.h5" Class="mb-2">Photonne también es una app</MudText>
        <MudText Typo="Typo.body1" Class="mud-text-secondary mb-4">
            Tus fotos están aquí mismo, en la web. Y para el día a día en el
            móvil (con copia de seguridad automática de la cámara) o en el
            escritorio, tienes la app nativa de Photonne.
        </MudText>

        <MudButton Href="/fotos"
                   Variant="Variant.Filled"
                   Color="Color.Primary"
                   StartIcon="@Icons.Material.Outlined.PhotoLibrary"
                   FullWidth="true"
                   Class="mb-2">
            Ir a mis fotos
        </MudButton>

        <MudButton Href="https://github.com/photonne/photonne"
                   Target="_blank"
                   Variant="Variant.Outlined"
                   Color="Color.Primary"
                   StartIcon="@Icons.Material.Outlined.GetApp"
                   FullWidth="true"
                   Class="mb-2">
            Descargar la app
        </MudButton>

        <MudButton OnClick="LogoutAsync"
                   Variant="Variant.Text"
                   Color="Color.Default"
                   StartIcon="@Icons.Material.Outlined.Logout"
                   FullWidth="true">
            Cerrar sesión
        </MudButton>
    </MudPaper>
</div>

@code {
    private async Task LogoutAsync()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("/login");
    }
}
