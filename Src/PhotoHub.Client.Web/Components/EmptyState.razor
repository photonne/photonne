<div class="d-flex flex-column align-center justify-center py-12 text-center @(string.IsNullOrWhiteSpace(Class) ? "" : Class)">
    <div style="opacity: 0.5;">
        <MudIcon Icon="@Icon"
                 Style="font-size: 56px; color: var(--mud-palette-text-disabled);" />
        <MudText Typo="Typo.body1" Class="mt-3">@Title</MudText>
        @if (!string.IsNullOrWhiteSpace(Description))
        {
            <MudText Typo="Typo.body2" Class="mud-text-secondary mt-1">@Description</MudText>
        }
    </div>
    @if (Actions is not null)
    {
        <div class="mt-4">
            @Actions
        </div>
    }
</div>

@code {
    [Parameter, EditorRequired] public string Icon { get; set; } = string.Empty;
    [Parameter, EditorRequired] public string Title { get; set; } = string.Empty;
    [Parameter] public string? Description { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter] public RenderFragment? Actions { get; set; }
}
