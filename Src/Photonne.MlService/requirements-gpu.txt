# GPU (NVIDIA/CUDA) variant of requirements.txt. Identical pins EXCEPT
# onnxruntime -> onnxruntime-gpu, which is what registers CUDAExecutionProvider.
# insightface and rapidocr-onnxruntime ride on whichever onnxruntime is
# installed, so pulling the -gpu wheel here moves faces + OCR to GPU too.
# Kept in lockstep with requirements.txt — update both together.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
insightface==2.0
onnxruntime-gpu==1.29.0
numpy==2.5.3
pillow==12.3.0
opencv-python-headless==5.0.0.93
rapidocr-onnxruntime==1.4.4
tokenizers==0.23.2
